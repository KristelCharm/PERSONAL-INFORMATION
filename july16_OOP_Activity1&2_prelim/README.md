# # 🧮 Total Calculator

A simple Java application that calculates and displays the sum of two predefined integer variables.

---

## 📌 Features

* Calculates the addition of two integer values (`x` and `y`).
* Prints the formatted total directly to the console output.